# FastAPI and web framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
jinja2==3.1.2
python-multipart==0.0.6

# Database
sqlalchemy[asyncio]==2.0.23
asyncpg==0.29.0
alembic==1.12.1

# Redis
redis[hiredis]==5.0.1

# HTTP client
httpx==0.25.2

# Security and encryption
cryptography==41.0.7
pyotp==2.9.0

# Scheduling
apscheduler==3.10.4

# LLM - Local models only (Ollama)
# openai and anthropic packages not needed for local-only setup
# Uncomment if you want to add cloud provider support later:
# openai==1.3.7
# anthropic==0.18.1

# Scientific computing
numpy==1.26.3

# Calendar integration
google-auth==2.23.4
google-auth-oauthlib==1.1.0
google-auth-httplib2==0.1.1
google-api-python-client==2.108.0

# Email
sendgrid==6.10.0

# Telephony
twilio==8.10.0

# Configuration
pydantic[email]==2.5.0
pydantic-settings==2.1.0
python-dotenv==1.0.0

# Logging
structlog==23.2.0

# WebSocket support (for coding environment)
websockets>=12.0

# System monitoring (for coding environment service management)
psutil>=5.9.0

# PDF generation
reportlab>=4.0.0

# PDF reading / extraction (for PDF Q&A and writer document uploads)
PyPDF2>=3.0.0

# Audio processing (required for TTS features)
soundfile>=0.12.1

# Text-to-Speech (TTS) - Optional, install separately based on Python version
# Note: TTS libraries have Python version constraints
#   For Python <3.12: pip install TTS>=0.22.0
#   For Python 3.12+: pip install piper-tts (or use alternative TTS solutions)
#   torch>=2.0.0 is required for Coqui TTS (install manually if using TTS)

# Development and testing
pytest==7.4.3
pytest-asyncio==0.21.1
httpx==0.25.2
vcrpy==6.0.1

# Monitoring (optional)
prometheus-client==0.19.0

# Neo4j Graph Database
neo4j==5.15.0

# System Dependencies (install via package manager, not pip):
# - docker-compose (REQUIRED for Neo4j)
#   Install via: sudo apt-get install docker-compose (Debian/Ubuntu)
#                sudo yum install docker-compose (RHEL/CentOS)
#                sudo apk add docker-compose (Alpine)
#   Or use Docker Desktop which includes Docker Compose V2
#   Note: The setup.sh script will attempt to install docker-compose automatically
